All the operations are implemented in prolog.

A) basic operations like:

	1) searching works like querying in database.
	2) searching in nested way.
	3) same number of 2 elements presence.
	4) swapping the prefix and suffix from given substring
	5) palindrome check.
	6) given sequence followed by new sequence and it recursive presence is a valid one checks its validity.

B) Boat problem, the most popular game safely transfereing combination of any 2 at a time from one end to onother end across river is implemted in prolog.  
